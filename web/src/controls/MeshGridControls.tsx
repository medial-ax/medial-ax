import { VineyardsGridMesh } from "mars_wasm";

export const MeshGridControls = ({ grid: _ }: { grid: VineyardsGridMesh }) => {
  return (
    <>
      <h3>Grid controls</h3>
      <span>Meshgrid has no options</span>
    </>
  );
};
