def make_medial_axis():
    # Get a grid covering the input shape

    # Find neighboring pairs so that we know where to check for knees

    # for every neighbor pair
    # . if there's a knee
    # . .  add the perp bisector to the medial axis
    pass


def kneebetween(p1, p2):
    # Add the complex using `p1` and `p2` as `key_points`s to a `vineyard` with `vineyard.add_complex`.
    # Call either `vineyard.is_knee` or `vineyard.is_dist_knee`.
    pass
